{
  "name": "@library/root",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run --exclude **/stress.long.test.ts",
    "test:watch": "vitest",
    "test:stress": "pnpm -F @gustavoqnt/ttl-cache test:stress",
    "typecheck": "tsc -b",
    "bench": "vitest bench",
    "clean": "pnpm -r run clean"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "tsup": "^8.3.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  },
  "engines": {
    "node": ">=18"
  }
}
